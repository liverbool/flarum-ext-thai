flarum-suspend:

  ##
  # UNIQUE KEYS - The following keys are used in only one location each.
  ##

  # Translations in this namespace are used by the admin interface.
  admin:

    # These translations are used in the Permissions page of the admin interface.
    permissions:
      suspend_users_label: Suspend users

  # Translations in this namespace are used by the forum user interface.
  forum:

    # These translations are used in the Suspend User modal dialog (admin function).
    suspend_user:
      indefinitely_label: ระงับไม่มีกำหนด
      limited_time_label: ระงับโดยกำหนดเวลา...
      limited_time_days_text: " วัน"
      not_suspended_label: ไม่มีผู้ถูกระงับ
      status_heading: สถานะการระงับ
      submit_button: => core.ref.save_changes
      title: "ระงับ {username}"

    # These translations are displayed as tooltips for user badges.
    user_badge:
      suspended_tooltip: ถูกระงับ

    # These translations are found on the user profile page (admin function).
    user_controls:
      suspend_button: ระงับ
